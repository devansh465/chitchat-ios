name: deep_link_router
description: "Navigator-key based warm-start deep link router for Flutter."
version: 0.1.0
publish_to: "none"

environment:
  sdk: ^3.5.4
  flutter: ">=1.17.0"

dependencies:
  app_links: ^6.4.0
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
